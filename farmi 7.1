Задача 1. Создать функцию max для определение максимального числа из 2 ЦЕЛОЧИСЛЕННЫХ параметров.

#include <stdio.h>
#include <string.h>
 Функция max

int max(int a, int b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
 return (a > b) ? a : b;
}
